# Actualiza paquetes
!sudo apt update

# Instala Ghostscript
!sudo apt install -y ghostscript

# Instala ViennaRNA
!sudo apt install -y viennarna

# Verifica instalación
!gs --version
!RNAfold --version
!RNAplot --version
